/**
 * Write a description of class Action here.
 * 
 * @author (David Florence) 
 * @version (.0.7.89)
 */
import java.util.Scanner; 
public class Action extends Person
{
    public Action()
    {
        // initialise instance variables
        x = 0;
    }
   public int Income()
   {
       //takes a coin
       /*currentPlayer.setTokens(CurrentTokens + 1)
        */
    }
   public static void foreignAid()
   {
       //takes 2 coins
       /*currentPlayer.setToken(CurrentTokens +2)
        * 
        */
    }
   public static void coup()
   {
       //kills a player's card 
       //uses 7 coins
       /* if currentPlayer.CurrentTokens >= 7
        * 
        * currentPlayer.setToken(CurrentTokens -7)
        * else 
        * System.out.println("Unavailable action");
        */
       
       //currentPlayer.setCoins(currentPlayer.getCoins()-7)
    }
   public int Tax()
    {
     //take 3 coins
       /* playerTokenCount 
        * 
        */
    }
   public void Assassinate()
   {
       //killa a card by paying 3 coins
       /*
        * 
        */ 
    }
   public void Exchange()
   {
       //exchange cards with court deck or another player
       /* 
        * 
        */
    }
    public int Steal()
    {
        //take 2 coins from another player
        /*
         *  currentPlayer.setTokens(CurrentTokens + 2)
         */
        
    } 
}

